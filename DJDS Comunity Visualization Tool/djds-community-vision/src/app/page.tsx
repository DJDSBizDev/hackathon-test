import { VisioningFlow } from "@/components/VisioningFlow";

/** Standalone entry — the full visioning flow with no session attached. */
export default function Home() {
  return <VisioningFlow />;
}
