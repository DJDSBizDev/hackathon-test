import { NewSessionForm } from "@/components/facilitator/NewSessionForm";

/** /facilitator/new — create a session, see the PIN once (BUILD-SPEC §9.1). */
export default function NewSessionPage() {
  return <NewSessionForm />;
}
